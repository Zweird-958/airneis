import type { Config } from "tailwindcss"

const config: Config = {
  content: [],
  theme: {
    extend: {
      borderRadius: {
        default: "0.5rem",
      },
    },
  },
}

export default config
